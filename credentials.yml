rest:
  # Canal REST para testes via curl ou Postman
  # Endpoint: POST http://localhost:5005/webhooks/rest/webhook
  # Body: { "sender": "usuario_teste", "message": "Converta 100 dólares para reais" }